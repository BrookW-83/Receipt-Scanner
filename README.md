# Receipt-Scanner

Scaffold repository for a new receipt-scanning feature that will integrate later with existing backend and mobile applications.

## Monorepo Layout

- `backend/`: Django + DRF service skeleton
- `mobile/receipt_scanner_mobile/`: Flutter module skeleton (feature-based)

This repo intentionally mirrors the structure style of your existing projects without reusing their existing domain features.
